# AI Agent Community Simulator

A lightweight multi-agent community simulation scaffold with FastAPI + Next.js.

## Structure
- `backend/`: FastAPI API, simulation engine, persistence models
- `frontend/`: Next.js UI scaffold
- `.github/workflows/`: CI skeleton

## Quickstart
```bash
cp backend/.env.example backend/.env
docker compose up --build
```
